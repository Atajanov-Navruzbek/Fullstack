let matn = "Salom dunyo";
let harf = matn.split("");
let count =0;
for (let i=0;i<=harf.length;i++){
    if (harf[i]==="a" || harf[i]==="e"|| harf[i]==="i" || harf[i]==="o" || harf[i]==="u"){
        count++
    }
};
console.log(count)